import {Arobase} from "../src";

describe("observer", () => {

    describe("#Arobase()", () => {
        it("should create an instance of modelSubject", () => {
            // const model = {name: "Joe"};
            // const arobase = new Arobase("testApp", model);
            // expect(arobase).toBeInstanceOf(Arobase);
        });
    });
});